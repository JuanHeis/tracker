---
phase: 10-persistence-ux-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - components/expense-tracker.tsx
  - components/resumen-card.tsx
  - components/patrimonio-card.tsx
  - components/config-card.tsx
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "All user-visible labels use standard personal finance terminology from REQUIREMENTS.md glossary"
    - "No occurrences of deprecated terms (salario, ingresos extras, saldo) in user-visible text"
    - "Terminology is consistent across all cards, dialogs, tables, tooltips, and error messages"
  artifacts:
    - path: "components/*.tsx"
      provides: "Consistent financial terminology across all components"
  key_links:
    - from: "REQUIREMENTS.md terminology"
      to: "all component user-visible strings"
      via: "systematic audit and replacement"
      pattern: "patrimonio|liquido|activo|pasivo|ingreso fijo|otros ingresos|egresos|disponible|cotizacion"
---

<objective>
Audit and standardize all user-visible financial terminology across the entire app.

Purpose: UX-01 requires standard personal finance terminology (patrimonio, liquido, activo, pasivo, ingreso fijo, otros ingresos, egresos, disponible, cotizacion) consistently throughout the app. While most terms were corrected in Phase 4 (ING-01), a systematic final audit ensures no inconsistencies remain in tooltips, error messages, placeholders, or dynamic strings.
Output: All components use consistent standard terminology.
</objective>

<execution_context>
@C:/Users/Juan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Juan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-persistence-ux-polish/10-RESEARCH.md

All component files under components/ directory (systematic audit target).
All hook files under hooks/ directory (for any user-visible strings in error messages).

Standard terminology glossary (from REQUIREMENTS.md):
- patrimonio (not "saldo total" or "total")
- liquido (not "disponible en cuenta" or "cash")
- activo (for loans given / investments)
- pasivo (for debts owed)
- ingreso fijo (not "salario" or "sueldo" in UI labels)
- otros ingresos (not "ingresos extras" or "extra income")
- egresos (for outflows: expenses + investment contributions)
- disponible (ingreso - egresos for the month)
- cotizacion (not "tipo de cambio" or "exchange rate" in Spanish UI)
- inversiones (not "investments" in Spanish UI)
- prestamos (loans given)
- deudas (debts owed)
- aguinaldo (bonus for dependientes)
- presupuesto (budget)
- recurrente (recurring)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Systematic terminology audit across all components</name>
  <files>components/expense-tracker.tsx, components/resumen-card.tsx, components/patrimonio-card.tsx, components/config-card.tsx</files>
  <action>
Perform a systematic audit of ALL user-visible strings in the codebase. This includes:

1. **Search for deprecated/inconsistent terms** using grep across all .tsx files:
   - "salario" or "Salario" (should be "Ingreso fijo" in labels, ok in code variable names)
   - "sueldo" or "Sueldo" (should be "Ingreso fijo" in labels)
   - "ingresos extras" or "Ingresos extras" (should be "Otros ingresos")
   - "saldo" when used as label (check context - "saldo" is ok for balance in some contexts)
   - "tipo de cambio" (should be "cotizacion" or "Cotizacion USD")
   - Any English terms in Spanish UI (e.g., "Balance", "Budget", "Income")

2. **Check all tooltip content** (strings inside Tooltip components or title attributes) for consistency.

3. **Check all placeholder text** in Input components for consistent terminology.

4. **Check all error messages** for consistent language.

5. **Check dialog titles** in all *-dialog.tsx files for consistent naming.

6. **Fix any inconsistencies found.** If the audit reveals the terminology is already correct (which is likely given Phase 4 work), document the findings and confirm compliance.

Important: Only change USER-VISIBLE strings (labels, tooltips, placeholders, error messages, dialog titles). Do NOT rename code variables, function names, type names, or localStorage keys -- those are internal and changing them would break the app.

Note: This task may result in zero or minimal changes if Phase 4 already handled everything. The value is the systematic verification that nothing was missed.
  </action>
  <verify>
    <automated>cd "D:/Documents/Programing/nextjs/expense-tracker" && grep -rn --include="*.tsx" -i "salario\|sueldo\|ingresos extras" components/ 2>&1 | grep -v "//\|console\|import\|interface\|type \|const \|function \|useState\|Props\|salaryHist\|salaryEntr\|SalaryEntry\|SalaryHistory\|salaryRes\|handleSalary\|setSalary\|getSalary\|useSalary\|salary" | head -20</automated>
  </verify>
  <done>All user-visible strings across the app use standard personal finance terminology. No deprecated terms appear in labels, tooltips, placeholders, or error messages. Audit documented with findings.</done>
</task>

<task type="auto">
  <name>Task 2: Final consistency pass on cards and summary components</name>
  <files>components/resumen-card.tsx, components/patrimonio-card.tsx</files>
  <action>
Verify and ensure the main financial summary components use precise terminology:

1. **Resumen card** (resumen-card.tsx):
   - "Ingresos" section header (green) -- verify present
   - "Ingreso fijo" line item -- verify (not "Salario")
   - "Otros ingresos" line item -- verify (not "Ingresos extras")
   - "Egresos" section header (red) -- verify present
   - "Gastos" and "Aportes inversiones" as sub-items -- verify
   - "Disponible" as the bottom-line calculation -- verify

2. **Patrimonio card** (patrimonio-card.tsx):
   - "Patrimonio Total" as main header -- verify
   - "Liquido ARS" and "Liquido USD" labels -- verify
   - "Inversiones" label -- verify
   - "Prestamos" (activos) and "Deudas" (pasivos) if displayed -- verify
   - Tooltip formulas use correct terms -- verify

3. If any inconsistency is found, fix it. If all is correct, this task confirms compliance.

4. Ensure that dynamic tooltip content (the formula breakdowns added in Phase 5) also uses consistent terms.
  </action>
  <verify>
    <automated>cd "D:/Documents/Programing/nextjs/expense-tracker" && grep -n "Ingreso fijo\|Otros ingresos\|Patrimonio\|Liquido\|Disponible\|Egresos" components/resumen-card.tsx components/patrimonio-card.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Resumen and Patrimonio cards confirmed to use standard terminology. All labels, tooltips, and dynamic content consistent with REQUIREMENTS.md glossary.</done>
</task>

</tasks>

<verification>
1. Grep for "salario" in user-visible strings -> zero hits (only code variable names)
2. Grep for "ingresos extras" -> zero hits
3. All card headers use standard terms: Patrimonio, Liquido, Disponible, Egresos, Ingresos
4. All dialog titles use Spanish financial terms consistently
5. All tooltips use correct terminology in their formulas
</verification>

<success_criteria>
- Zero deprecated terms in user-visible strings across the entire app
- Standard glossary terms used consistently in all cards, dialogs, tables, and tooltips
- Terminology audit documented with specific findings
</success_criteria>

<output>
After completion, create `.planning/phases/10-persistence-ux-polish/10-03-SUMMARY.md`
</output>
