---
phase: 10-persistence-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/investment-dialog.tsx
  - components/recurring-dialog.tsx
  - components/budget-dialog.tsx
  - components/usd-purchase-dialog.tsx
autonomous: true
requirements:
  - UX-02

must_haves:
  truths:
    - "User submitting investment dialog with amount 0 or negative sees red error message and form does not submit"
    - "User submitting recurring dialog with empty name, amount <= 0, or no category sees red error messages for each invalid field"
    - "User submitting budget dialog with limit <= 0 sees red error message"
    - "User sees descriptive error text near invalid fields in USD purchase dialog when amounts are invalid"
  artifacts:
    - path: "components/investment-dialog.tsx"
      provides: "Amount > 0 validation with error display"
    - path: "components/recurring-dialog.tsx"
      provides: "Name, amount > 0, category validation with error display"
    - path: "components/budget-dialog.tsx"
      provides: "Limit > 0 validation with error display"
    - path: "components/usd-purchase-dialog.tsx"
      provides: "Error text display when amounts invalid"
  key_links:
    - from: "all dialog components"
      to: "validation pattern"
      via: "errors state + red text display (matches loan-dialog.tsx pattern)"
      pattern: "setErrors|text-red-500"
---

<objective>
Add consistent form validation with visible error messages to all dialogs that currently have silent failures or missing validation.

Purpose: UX-02 requires every form to validate amount > 0 and USD rate > 0 before submission. Several dialogs either silently return on invalid input or have no validation at all.
Output: All monetary form dialogs show red error text when validation fails.
</objective>

<execution_context>
@C:/Users/Juan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Juan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-persistence-ux-polish/10-RESEARCH.md

@components/loan-dialog.tsx (REFERENCE: gold standard validation pattern to replicate)
@components/investment-dialog.tsx (NO validation - needs full add)
@components/recurring-dialog.tsx (silent return - needs error display)
@components/budget-dialog.tsx (silent return - needs error display)
@components/usd-purchase-dialog.tsx (button disabled but no error text)

<interfaces>
<!-- Validation pattern from loan-dialog.tsx (the gold standard to replicate) -->

From components/loan-dialog.tsx:
```typescript
// State for errors
const [errors, setErrors] = useState<Record<string, string>>({});

// Validation in handleSubmit
const handleSubmit = (e: React.FormEvent<HTMLFormElement>) => {
  e.preventDefault();
  const newErrors: Record<string, string> = {};

  if (isNaN(parsedAmount) || parsedAmount <= 0) {
    newErrors.amount = "Debe ser mayor a 0";
  }

  if (Object.keys(newErrors).length > 0) {
    setErrors(newErrors);
    return;
  }
  // proceed...
};

// Display pattern in JSX
{errors.amount && (
  <p className="text-xs text-red-500">{errors.amount}</p>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validation to investment-dialog and recurring-dialog</name>
  <files>components/investment-dialog.tsx, components/recurring-dialog.tsx</files>
  <action>
**investment-dialog.tsx:**

1. Add `const [errors, setErrors] = useState<Record<string, string>>({});` state.
2. Clear errors when dialog opens (in useEffect or when `open` changes).
3. In `handleSubmit`, before `onAdd()` call (the create path, NOT the edit path):
   - Parse amount: `const amount = Number(formData.get("amount"));`
   - Validate: `if (isNaN(amount) || amount <= 0)` -> set `newErrors.amount = "El monto debe ser mayor a 0"`
   - If errors exist, `setErrors(newErrors); return;`
4. Add `{errors.amount && <p className="text-xs text-red-500">{errors.amount}</p>}` below the amount Input field.
5. For Plazo Fijo TNA field: validate TNA > 0 if isPlazoFijo. Error: "La TNA debe ser mayor a 0".

**recurring-dialog.tsx:**

1. Add `const [errors, setErrors] = useState<Record<string, string>>({});` state.
2. Clear errors when dialog opens.
3. Replace the silent `if (!name || amount <= 0 || !selectedCategory) return;` with explicit validation:
   - `if (!name.trim())` -> `newErrors.name = "El nombre es requerido"`
   - `if (isNaN(amount) || amount <= 0)` -> `newErrors.amount = "El monto debe ser mayor a 0"`
   - `if (!selectedCategory)` -> `newErrors.category = "Selecciona una categoria"`
   - If errors: `setErrors(newErrors); return;`
4. Add `<p className="text-xs text-red-500">` error display below each respective field (name input, amount input, category select).
  </action>
  <verify>
    <automated>cd "D:/Documents/Programing/nextjs/expense-tracker" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Investment dialog rejects amount <= 0 and TNA <= 0 (for PF) with red error text. Recurring dialog shows specific red error messages for empty name, invalid amount, and missing category instead of silently failing.</done>
</task>

<task type="auto">
  <name>Task 2: Add validation display to budget-dialog and usd-purchase-dialog</name>
  <files>components/budget-dialog.tsx, components/usd-purchase-dialog.tsx</files>
  <action>
**budget-dialog.tsx:**

1. Add `const [errors, setErrors] = useState<Record<string, string>>({});` state.
2. Clear errors when dialog opens.
3. Replace the silent `if (limit <= 0 || isNaN(limit)) return;` with:
   - `if (isNaN(limit) || limit <= 0)` -> `newErrors.limit = "El limite debe ser mayor a 0"`
   - `if (!category)` -> `newErrors.category = "Selecciona una categoria"`
   - If errors: `setErrors(newErrors); return;`
4. Add red error text below the limit input and category select.

**usd-purchase-dialog.tsx:**

1. The dialog already has `isValid` that disables the submit button. Add visible error text that appears when user has interacted with fields but values are invalid.
2. Add error text below amount fields:
   - Below ARS amount: if arsAmount is entered and <= 0, show "El monto ARS debe ser mayor a 0"
   - Below USD amount: if usdAmount is entered and <= 0, show "El monto USD debe ser mayor a 0"
3. Use conditional display: only show errors after user has typed something (check if field value is non-empty and invalid), so errors don't show on initial empty state.
4. Use same `<p className="text-xs text-red-500">` pattern for consistency.
  </action>
  <verify>
    <automated>cd "D:/Documents/Programing/nextjs/expense-tracker" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Budget dialog shows red error for limit <= 0 and missing category. USD purchase dialog shows inline red errors when entered amounts are invalid. All 4 dialogs now match the loan-dialog validation pattern with visible error messages.</done>
</task>

</tasks>

<verification>
1. Open investment dialog, submit with amount 0 -> red error shown, form does not submit
2. Open investment dialog, enter PF type with TNA 0 -> red error shown
3. Open recurring dialog, submit with all fields empty -> 3 red error messages shown
4. Open budget dialog, enter limit -5 -> red error shown, form does not submit
5. In USD purchase dialog, type 0 in ARS field -> error text appears below field
6. All error messages use consistent "text-xs text-red-500" styling
</verification>

<success_criteria>
- Every monetary form dialog shows visible red error text when validation fails
- No dialog silently returns without user feedback on invalid input
- Validation pattern consistent across all dialogs (errors state + red text)
- All amount fields reject <= 0 values
</success_criteria>

<output>
After completion, create `.planning/phases/10-persistence-ux-polish/10-02-SUMMARY.md`
</output>
