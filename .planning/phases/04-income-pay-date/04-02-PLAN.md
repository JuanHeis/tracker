---
phase: 04-income-pay-date
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - components/salary-card.tsx
  - hooks/useSalaryHistory.ts
autonomous: true
requirements: [ING-02, ING-06, ING-08]

must_haves:
  truths:
    - "User can see and edit employment type (dependiente/independiente) in the salary card"
    - "User can see and edit pay day (1-31) in the salary card"
    - "User sees a timeline of salary changes in the salary card (most recent first)"
    - "User can click to edit any salary history entry"
    - "User can add a new salary entry with effective date"
    - "Salary increases from a past entry propagate to all months until the next entry"
  artifacts:
    - path: "components/salary-card.tsx"
      provides: "Employment config display/edit, salary timeline, salary entry editing"
      contains: "employmentType"
  key_links:
    - from: "components/salary-card.tsx"
      to: "hooks/useSalaryHistory.ts"
      via: "props from useMoneyTracker"
      pattern: "salaryHistory|incomeConfig|addSalaryEntry"
---

<objective>
Refactor the salary card to display employment configuration (dependiente/independiente + pay day) with inline editing, and add a salary history timeline showing effective-date entries with click-to-edit capability.

Purpose: Users need to configure their employment type and pay date, and see/manage their salary history as a timeline rather than editing per-month values.
Output: Fully refactored salary-card.tsx with employment config section and salary timeline.
</objective>

<execution_context>
@C:/Users/Juan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Juan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-income-pay-date/04-RESEARCH.md
@.planning/phases/04-income-pay-date/04-01-SUMMARY.md

<interfaces>
<!-- From 04-01: useSalaryHistory types -->
```typescript
export interface SalaryEntry {
  id: string;
  effectiveDate: string;  // "yyyy-MM"
  amount: number;
  usdRate: number;
}

export interface IncomeConfig {
  employmentType: "dependiente" | "independiente";
  payDay: number;  // 1-31
}

// Hook returns:
// salaryHistory, incomeConfig, getSalaryForMonth, 
// addSalaryEntry, updateSalaryEntry, deleteSalaryEntry, setIncomeConfig
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add employment config section to salary card</name>
  <files>components/salary-card.tsx</files>
  <action>
Add an employment configuration section at the top of the salary card (below the header, above the salary display).

**Props to add to SalaryCardProps:**
- `incomeConfig: IncomeConfig`
- `onUpdateIncomeConfig: (config: IncomeConfig) => void`

**Employment config display (always visible):**
- Row: "Tipo:" with value "Dependiente" or "Independiente" + pencil icon to edit
- Row: "Dia de cobro:" with value (e.g., "10") + pencil icon to edit
- Use the established click-to-edit pattern (pencil icon, inline input, check/X buttons)

**Edit mode for employment type:**
- Two-button toggle or Select dropdown: "Dependiente" / "Independiente"
- On change, call `onUpdateIncomeConfig({ ...incomeConfig, employmentType: value })`

**Edit mode for pay day:**
- Number input, validated 1-31
- On confirm, clamp value with `Math.max(1, Math.min(31, value))` then call `onUpdateIncomeConfig`

**Styling:**
- Compact section with `text-sm` styling
- Subtle divider (hr) between config section and salary display
- Pencil icons use `text-muted-foreground hover:text-blue-500` pattern from Phase 2/3
  </action>
  <verify>
    <automated>cd D:/Documents/Programing/nextjs/expense-tracker && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Salary card shows employment type and pay day with pencil-to-edit. Values persist via incomeConfig in localStorage.</done>
</task>

<task type="auto">
  <name>Task 2: Add salary history timeline with click-to-edit</name>
  <files>components/salary-card.tsx</files>
  <action>
Replace the simple salary display/edit form with a salary history timeline section.

**Props to add to SalaryCardProps:**
- `salaryHistory: SalaryEntry[]`
- `onAddSalaryEntry: (entry: Omit<SalaryEntry, 'id'>) => void`
- `onUpdateSalaryEntry: (id: string, updates: Partial<SalaryEntry>) => void`
- `onDeleteSalaryEntry: (id: string) => void`
- `currentMonthSalary: { amount: number; usdRate: number; isOverride: boolean }`

**Salary display (current month):**
- Show "Ingreso fijo: $X" with the resolved salary for the current month
- If `isOverride`, show a small badge/indicator (e.g., "(ajuste)" in muted text)
- Below, show formatted amount using FormattedAmount component

**Salary timeline:**
- Section titled "Historial" (small, collapsible or always visible)
- List entries sorted most-recent-first: "Desde Ene 2026: $500.000" format
- Use `format(parse(effectiveDate, 'yyyy-MM', new Date()), 'MMM yyyy', { locale: es })` for display
- Each entry has pencil icon for click-to-edit
- Click-to-edit: inline inputs for amount and usdRate, with check/X buttons
- On save, call `onUpdateSalaryEntry(id, { amount, usdRate })`

**Add new salary entry:**
- Button "+ Nuevo ingreso fijo" below timeline
- Opens inline form: effective date (month picker or yyyy-MM input), amount, usdRate
- On save, call `onAddSalaryEntry({ effectiveDate, amount, usdRate })`
- Effective date defaults to current month

**Remove the old salary form:**
- Delete the old `showSalaryForm` conditional form that wrote to `monthlyData.salaries`
- The "Editar Ingreso fijo" button at the bottom becomes unnecessary — editing happens inline in timeline
- Keep the `showSalaryForm` prop removal clean — update SalaryCardProps

**Wire in expense-tracker.tsx:**
- Pass the new props from useMoneyTracker to SalaryCard
- Remove the old `onSalarySubmit` and `showSalaryForm` props
  </action>
  <verify>
    <automated>cd D:/Documents/Programing/nextjs/expense-tracker && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Salary card shows current month resolved salary, salary history timeline with click-to-edit, and "add new entry" form. Old per-month salary form removed.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Salary card shows employment type (dependiente/independiente) with edit capability
3. Salary card shows pay day with edit capability (validated 1-31)
4. Salary history timeline displays entries sorted most-recent-first
5. User can click-to-edit any salary entry in the timeline
6. User can add a new salary entry with effective date
7. Current month salary resolves correctly from history
</verification>

<success_criteria>
- Employment config editable inline in salary card
- Pay day validated 1-31 and persisted
- Salary timeline shows all history entries with dates
- Click-to-edit works on timeline entries
- New salary entries can be added with effective date
- Old salary form removed, replaced by timeline-based editing
</success_criteria>

<output>
After completion, create `.planning/phases/04-income-pay-date/04-02-SUMMARY.md`
</output>
