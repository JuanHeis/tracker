---
phase: 14-recharts-upgrade-chart-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - components/charts/salary-by-month.tsx
autonomous: true
requirements: [INFRA-01, INFRA-03]
must_haves:
  truths:
    - "Recharts 3.x is installed (^3.8.1 in package.json)"
    - "Existing charts compile without errors after upgrade"
    - "No unused recharts imports remain in chart files"
    - "No localStorage interfaces were modified (INFRA-03 invariant)"
  artifacts:
    - path: "package.json"
      provides: "Recharts 3.x dependency"
      contains: "recharts.*3"
    - path: "components/charts/salary-by-month.tsx"
      provides: "Clean imports (no unused ResponsiveContainer/Tooltip)"
  key_links:
    - from: "components/charts/salary-by-month.tsx"
      to: "recharts"
      via: "import { Bar, BarChart, XAxis, YAxis }"
      pattern: "from \"recharts\""
    - from: "components/charts/expenses-by-month.tsx"
      to: "recharts"
      via: "import { Bar, BarChart, XAxis, YAxis }"
      pattern: "from \"recharts\""
---

<objective>
Upgrade Recharts from 2.13.3 to 3.x and verify all existing charts still compile and render correctly.

Purpose: INFRA-01 requires Recharts 3.x with verified existing charts. INFRA-03 requires zero changes to localStorage interfaces — this plan must confirm that constraint by only touching the recharts dependency and chart component imports.
Output: Updated package.json with recharts ^3.8.1, clean chart imports, passing build.
</objective>

<execution_context>
@C:/Users/Juan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Juan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-recharts-upgrade-chart-infrastructure/14-RESEARCH.md

<interfaces>
<!-- Existing chart imports that need verification post-upgrade -->

From components/charts/salary-by-month.tsx (CURRENT — has unused imports):
```typescript
import { Bar, BarChart, ResponsiveContainer, XAxis, YAxis, Tooltip } from "recharts";
// ResponsiveContainer and Tooltip are UNUSED — ChartContainer and ChartTooltip from shadcn are used instead
```

From components/charts/expenses-by-month.tsx (CURRENT — clean imports):
```typescript
import { Bar, BarChart, XAxis, YAxis } from "recharts";
// Already clean — no changes needed
```

From components/ui/chart.tsx (DO NOT MODIFY):
```typescript
import * as RechartsPrimitive from "recharts";
// Uses ResponsiveContainer, Tooltip, Legend internally — stable API across 2.x→3.x
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Recharts to 3.x and clean unused imports</name>
  <files>package.json, package-lock.json, components/charts/salary-by-month.tsx</files>
  <action>
1. Run `npm install recharts@^3.8.1` to upgrade from 2.13.3 to 3.x.

2. In `components/charts/salary-by-month.tsx`, replace the recharts import:
   FROM: `import { Bar, BarChart, ResponsiveContainer, XAxis, YAxis, Tooltip } from "recharts";`
   TO: `import { Bar, BarChart, XAxis, YAxis } from "recharts";`
   
   `ResponsiveContainer` and `Tooltip` are unused — the component uses `ChartContainer` (which wraps ResponsiveContainer internally) and `ChartTooltip` from shadcn instead.

3. Do NOT touch `components/charts/expenses-by-month.tsx` — its imports are already clean.
4. Do NOT touch `components/ui/chart.tsx` — it uses stable `* as RechartsPrimitive` import.
5. Do NOT touch any hooks, types, or localStorage-related files (INFRA-03 invariant).
  </action>
  <verify>
    <automated>cd D:/Documents/Programing/nextjs/expense-tracker && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>package.json shows recharts ^3.8.1, salary-by-month.tsx has no unused recharts imports, `npm run build` succeeds with zero errors</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <what-built>Recharts upgraded from 2.13.3 to 3.x. Existing charts (Ingreso fijo por Mes, Gastos por Mes) should render identically. Unused imports cleaned from salary-by-month.tsx.</what-built>
  <how-to-verify>
1. Run `npm run dev` and open the app
2. Navigate to the charts section (Graficos tab)
3. Verify "Gastos por Mes" bar chart renders with correct bars and tooltips
4. Verify "Ingreso fijo por Mes" bar chart renders with ARS/USD tabs working
5. Switch between ARS and USD tabs — both should show correct data
6. Hover over bars — tooltips should appear with formatted values
7. Test JSON export/import (Configuracion > Exportar > download, then Importar > select file) — data should round-trip without loss (INFRA-03 regression check)
  </how-to-verify>
  <resume-signal>Type "approved" if charts render correctly and export/import works, or describe any visual regressions</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- `grep "recharts" package.json` shows version ^3.8.1
- `grep "ResponsiveContainer\|Tooltip" components/charts/salary-by-month.tsx` returns NO matches (unused imports removed)
- No files in `hooks/` or `types/` directories were modified
</verification>

<success_criteria>
Recharts 3.x is installed, build passes, existing BarCharts compile without errors, unused imports cleaned, and zero localStorage interfaces touched.
</success_criteria>

<output>
After completion, create `.planning/phases/14-recharts-upgrade-chart-infrastructure/14-01-SUMMARY.md`
</output>
