---
phase: 02-investment-model-refactor
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - components/expense-tracker.tsx
  - hooks/useMoneyTracker.ts
autonomous: false
requirements: [INV-05, INV-06, INV-07, INV-08, INV-10]

must_haves:
  truths:
    - "User can create a Plazo Fijo and sees auto-calculated value that grows with elapsed days"
    - "User can create a non-PF investment, add movements, update value inline, and see correct gain/loss"
    - "User can finalize an investment — auto-retiro created, status changes to Finalizada, value zeroed, row dimmed"
    - "Finalized investments show final gain/loss and percentage (historical performance)"
    - "Desactualizado badge appears on investments not updated in 7+ days (not on PF or Finalizada)"
    - "Investment table shows all expected columns with correct data"
  artifacts:
    - path: "components/expense-tracker.tsx"
      provides: "Fully integrated investment system"
    - path: "hooks/useMoneyTracker.ts"
      provides: "Complete investment operations exposed through hook"
  key_links:
    - from: "components/investments-table.tsx"
      to: "components/investment-row.tsx"
      via: "Full row rendering with all features"
    - from: "hooks/useMoneyTracker.ts"
      to: "hooks/useInvestmentsTracker.ts"
      via: "All investment operations pass through"
---

<objective>
End-to-end verification of the complete investment model refactor. Fix any integration issues, ensure all features work together, and verify via user acceptance testing.

Purpose: Confirm all INV requirements are met and the system works as a cohesive whole.
Output: Verified working investment system.
</objective>

<execution_context>
@C:/Users/Juan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Juan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-investment-model-refactor/02-04-SUMMARY.md
@.planning/phases/02-investment-model-refactor/02-CONTEXT.md

@hooks/useMoneyTracker.ts
@hooks/useInvestmentsTracker.ts
@components/investments-table.tsx
@components/investment-row.tsx
@components/investment-movements.tsx
@components/investment-value-cell.tsx
@components/investment-dialog.tsx
@components/expense-tracker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration fixes and edge case handling</name>
  <files>components/expense-tracker.tsx, hooks/useMoneyTracker.ts</files>
  <action>
Run the app and fix any integration issues between the 4 prior plans. Common issues to check and fix:

1. **Build check:** Run `npm run build` — fix any TypeScript or build errors.

2. **useMoneyTracker return object completeness:** Ensure all new investment operations (handleAddMovement, handleDeleteMovement, handleUpdateValue, handleFinalizeInvestment) are included in the return object and properly forwarded from useInvestmentsTracker.

3. **calculateTotalAvailable correctness:** Verify the movement-based calculation works. An aporte in month X reduces liquidity in month X. A retiro (including finalization retiro) in month Y adds to liquidity in month Y. The `total` field should NOT include investment impact (per Phase 1 decision: investments are blocked money, not income).

4. **Existing data backward compatibility:** Ensure migrateData handles edge cases:
   - Investment with no `date` field (shouldn't happen but be defensive)
   - Investment with `amount: 0`
   - Investment with missing `status` field

5. **PF Vencido badge edge case:** If startDate or plazoDias is missing on a PF, don't show Vencido badge (return false from isPlazoFijoVencido).

6. **Finalization edge case:** When finalizing an investment whose currentValue is 0 (user already withdrew manually), the auto-retiro should be for 0 and should still transition to Finalizada.

7. **Currency symbol in displays:** Ensure amounts show with correct currency prefix ($ for ARS, US$ for USD) based on investment.currencyType. Check that FormattedAmount or toLocaleString handles this.
  </action>
  <verify>
    <automated>cd D:/Documents/Programing/nextjs/expense-tracker && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>App builds without errors. All investment operations work end-to-end. Edge cases handled defensively.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete investment model refactor — investments now behave as accounts with movements, inline value editing, Plazo Fijo auto-calculation, finalization, and performance metrics.</what-built>
  <how-to-verify>
Run `npm run dev` and test these scenarios in the browser:

1. **Create a Plazo Fijo:**
   - Click "Inversion" button
   - Select type "Plazo Fijo" — verify TNA and Plazo fields appear, currency locked to ARS
   - Enter: Name "PF Banco Nacion", Amount 100000, TNA 75, Plazo 30
   - Submit — verify it appears in table with auto-calculated value > 100000

2. **Create a Crypto investment:**
   - Click "Inversion", select "Crypto" — verify currency locked to USD
   - Enter: Name "Bitcoin", Amount 500
   - Submit — verify it appears with USD currency

3. **Add movements:**
   - Click the Bitcoin row to expand it — verify movement history shows initial aporte
   - Add a new aporte: amount 200, type Aporte — verify it appears in list
   - Verify Capital Invested column updates to 700

4. **Update value inline:**
   - Click the "Valor Actual" cell on Bitcoin row
   - Change to 800, press Enter
   - Verify gain/loss shows +$100 (+14.3% approximately)

5. **Check outdated warning:**
   - The Bitcoin investment was just updated, so no Desactualizado badge should appear
   - (The PF should never show it either since it's auto-calculated)

6. **Finalize an investment:**
   - Click "Finalizar" on Bitcoin row
   - Verify confirmation dialog shows: "Finalizar Bitcoin? Se creara un retiro por $800..."
   - Confirm — verify row becomes dimmed, shows "Finalizada" badge, value is 0
   - Verify the retiro movement was auto-created

7. **Month switching:**
   - Switch to a different month — verify investments still appear (they're persistent accounts)
   - Only movements are month-specific for liquidity calculation

8. **Verify liquidity impact:**
   - Check "Disponible este mes" card — investment aportes should reduce available money for the month they were created in
  </how-to-verify>
  <resume-signal>Type "approved" if all scenarios pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. All 5 roadmap success criteria for Phase 2 are met:
   - SC1: User can create investment account and add/remove money via movements
   - SC2: User can update value inline and finalize (auto-retiro + status change)
   - SC3: Gain/loss amount and % visible, "value outdated" warning present
   - SC4: PF auto-calculates, Crypto forces USD, FCI allows choice, Acciones follow market
   - SC5: Table displays all required columns with data
</verification>

<success_criteria>
- App builds and runs without errors
- All investment CRUD operations work end-to-end
- PF auto-calculation correct (simple interest formula)
- Currency enforcement working per type
- Finalization creates retiro and transitions correctly
- Human verifies all scenarios pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-investment-model-refactor/02-05-SUMMARY.md`
</output>
