---
phase: 02-investment-model-refactor
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/investment-dialog.tsx
  - components/expense-tracker.tsx
autonomous: true
requirements: [INV-01, INV-08, INV-09]

must_haves:
  truths:
    - "Investment creation dialog shows name, type, currency, and initial amount fields"
    - "When type is Plazo Fijo, TNA (%) and Plazo (dias) fields appear; currency is forced to ARS and disabled"
    - "When type is Crypto, currency is forced to USD and disabled"
    - "When type is FCI or Acciones, currency select is enabled for user choice (ARS/USD)"
    - "Dialog submits data in the new format expected by refactored handleAddInvestment"
  artifacts:
    - path: "components/investment-dialog.tsx"
      provides: "Updated dialog with conditional PF fields and currency enforcement"
      contains: "tna"
    - path: "components/expense-tracker.tsx"
      provides: "Updated wiring between dialog and new hook API"
      contains: "handleAddInvestment"
  key_links:
    - from: "components/investment-dialog.tsx"
      to: "constants/investments.ts"
      via: "imports CURRENCY_ENFORCEMENT for type-to-currency mapping"
      pattern: "CURRENCY_ENFORCEMENT"
    - from: "components/expense-tracker.tsx"
      to: "hooks/useMoneyTracker.ts"
      via: "destructures new investment operations from useMoneyTracker"
      pattern: "handleAddMovement|handleFinalizeInvestment|handleUpdateValue|handleUpdatePFFields"
---

<objective>
Update the investment creation dialog with conditional Plazo Fijo fields (TNA, plazo) and currency enforcement per investment type. Wire the dialog to the new handleAddInvestment API. Update expense-tracker.tsx to pass new hook functions through.

Purpose: Users can create investment accounts with type-specific behavior and enforced currency rules.
Output: Updated investment dialog and expense-tracker wiring.
</objective>

<execution_context>
@C:/Users/Juan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Juan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-investment-model-refactor/02-01-SUMMARY.md
@.planning/phases/02-investment-model-refactor/02-RESEARCH.md

@components/investment-dialog.tsx
@components/expense-tracker.tsx
@constants/investments.ts
@hooks/useMoneyTracker.ts

<interfaces>
<!-- New Investment creation API from 02-01 -->
handleAddInvestment expects:
```typescript
{
  name: string;
  type: InvestmentType;
  currencyType: CurrencyType;
  initialAmount: number;
  date: string;
  tna?: number;       // Only for Plazo Fijo
  plazoDias?: number; // Only for Plazo Fijo
}
```

From constants/investments.ts (after 02-01):
```typescript
export const CURRENCY_ENFORCEMENT: Record<InvestmentType, CurrencyType | null> = {
  "Crypto": CurrencyType.USD,
  "Plazo Fijo": CurrencyType.ARS,
  "FCI": null,
  "Acciones": null,
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update InvestmentDialog with PF fields and currency enforcement</name>
  <files>components/investment-dialog.tsx</files>
  <action>
Rewrite InvestmentDialog to support the new creation model:

1. **Add local state for type selection** using useState (needed to conditionally show PF fields and enforce currency). Use controlled Select for type instead of uncontrolled.

2. **Currency enforcement:** When type changes, check CURRENCY_ENFORCEMENT map. If non-null, set currency to enforced value and disable the currency Select. If null, enable currency Select.

3. **Conditional Plazo Fijo fields:** When type === "Plazo Fijo", show two additional inputs:
   - TNA (%): `<Input type="number" name="tna" placeholder="TNA anual (%)" step="0.01" required />`
   - Plazo (dias): `<Input type="number" name="plazoDias" placeholder="Plazo (dias)" min="1" required />`

4. **Remove old fields that no longer exist in the model:**
   - Remove `usdRate` input (not part of investment model anymore — currency is tracked on the investment itself)
   - Remove `expectedEndDate` input (replaced by PF-specific plazoDias for PF; other types don't have end date)

5. **Update form submission:** The dialog should call `onSubmit` with the form event. The parent (expense-tracker.tsx) will extract FormData and call the new `handleAddInvestment` with the correct shape. Alternatively, change the dialog to handle extraction internally and call a callback with the structured data. Choose the approach that minimizes changes — a callback with structured data is cleaner given the new API shape:

```typescript
interface InvestmentDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onSubmit: (data: {
    name: string;
    type: InvestmentType;
    currencyType: CurrencyType;
    initialAmount: number;
    date: string;
    tna?: number;
    plazoDias?: number;
  }) => void;
  onClose: () => void;
  defaultInvestmentDate: string;
  editingInvestment: Investment | null;
}
```

When `editingInvestment` is provided, pre-fill fields from the investment. For editing, only name and type-specific fields (tna, plazoDias) should be editable — type and currency are locked after creation. The dialog title changes to "Editar Inversion".

6. **Form layout:** Keep the existing Dialog/DialogContent/DialogHeader structure. Fields stacked vertically: Date, Name, Amount (initial aporte), Type selector, Currency selector (disabled when enforced), and conditionally TNA + Plazo for PF.
  </action>
  <verify>
    <automated>cd D:/Documents/Programing/nextjs/expense-tracker && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Dialog shows conditional PF fields, enforces currency per type, submits structured data matching new API. usdRate and expectedEndDate fields removed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire expense-tracker.tsx to new investment hook API</name>
  <files>components/expense-tracker.tsx</files>
  <action>
Update expense-tracker.tsx to pass the new investment operations through:

1. **Destructure new functions from useMoneyTracker:** Add `handleAddMovement`, `handleUpdateValue`, `handleFinalizeInvestment`, `handleDeleteMovement`, `handleUpdatePFFields` to the destructured return. These will be passed to the InvestmentsTable in plan 02-04.

2. **Update InvestmentDialog usage:** The `onSubmit` prop now receives structured data (not a form event). Wrap `handleAddInvestment` or pass it directly since it now accepts the structured object. For editing, pass a separate handler or adapt the existing one.

3. **Remove investment-related properties that no longer exist:** The old `handleUpdateInvestment` (form-event based) is replaced by the structured API. Clean up any references.

4. **Keep the "Inversion" button** that opens the dialog. Keep the dialog placement at the bottom of the component.

5. **Pass new functions to InvestmentsTable** (these will be consumed in plan 02-04, but wiring them now avoids TypeScript errors). For now, InvestmentsTable still uses old props — add the new props alongside old ones and let plan 02-04 update the component. If this causes type errors, temporarily use `as any` with a TODO comment referencing plan 02-04, or update InvestmentsTable props interface to accept the new functions optionally.

NOTE: The InvestmentsTable component will be fully rewritten in plan 02-04. Keep current usage working — pass `filteredInvestments` (which now returns all investments sorted by active/finalized from the refactored hook).
  </action>
  <verify>
    <automated>cd D:/Documents/Programing/nextjs/expense-tracker && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>expense-tracker.tsx passes new investment operations through, InvestmentDialog receives structured onSubmit callback, app compiles without errors</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. InvestmentDialog shows TNA + Plazo fields when Plazo Fijo is selected
3. Currency select is disabled and shows "ARS" for Plazo Fijo, "USD" for Crypto
4. Currency select is enabled for FCI and Acciones
5. Dialog submits structured data (not raw FormEvent) to parent
6. expense-tracker.tsx compiles with new hook API
</verification>

<success_criteria>
- Type selection controls conditional field visibility (PF shows TNA + Plazo)
- Currency enforcement works per CURRENCY_ENFORCEMENT map
- Dialog and expense-tracker compile against new investment hook API
- Old usdRate and expectedEndDate fields removed from dialog
</success_criteria>

<output>
After completion, create `.planning/phases/02-investment-model-refactor/02-02-SUMMARY.md`
</output>
