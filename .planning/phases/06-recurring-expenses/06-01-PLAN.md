---
phase: 06-recurring-expenses
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/useMoneyTracker.ts
  - hooks/useRecurringExpenses.ts
  - constants/colors.ts
autonomous: true
requirements: [REC-01, REC-02, REC-03]

must_haves:
  truths:
    - "RecurringExpense interface exists with name, amount, category, currencyType, status, createdAt fields"
    - "Category type includes Seguros, Impuestos, Transporte, Salud with colors"
    - "Expense interface has optional recurringId and isPaid fields"
    - "useRecurringExpenses hook provides CRUD for recurring definitions and auto-generation"
    - "Auto-generation creates Expense instances for all active recurrings from createdAt to current month"
    - "Paused recurrings stop generating from pausedAt month onward"
  artifacts:
    - path: "hooks/useRecurringExpenses.ts"
      provides: "Recurring expense CRUD + auto-generation logic"
      exports: ["useRecurringExpenses", "RecurringExpense", "RecurringStatus"]
    - path: "hooks/useMoneyTracker.ts"
      provides: "Extended Category type, Expense interface with recurringId/isPaid, wiring to useRecurringExpenses"
    - path: "constants/colors.ts"
      provides: "4 new category colors"
  key_links:
    - from: "hooks/useRecurringExpenses.ts"
      to: "hooks/useMoneyTracker.ts"
      via: "useRecurringExpenses called from useMoneyTracker, receives monthlyData and globalUsdRate"
      pattern: "useRecurringExpenses"
---

<objective>
Create the RecurringExpense data model, extend Expense interface with recurringId/isPaid, add 4 new categories, and implement the useRecurringExpenses hook with auto-generation logic.

Purpose: Establishes the data foundation for recurring expenses — the template+instance model, CRUD operations, and auto-generation that fills missing months on app load.
Output: useRecurringExpenses.ts hook, extended types in useMoneyTracker.ts, new categories in colors.ts
</objective>

<execution_context>
@C:/Users/Juan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Juan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-recurring-expenses/06-CONTEXT.md
@.planning/phases/06-recurring-expenses/06-RESEARCH.md

@hooks/useMoneyTracker.ts
@hooks/useExpensesTracker.ts
@hooks/useLocalStorage.ts
@constants/colors.ts

<interfaces>
<!-- Current Expense interface from hooks/useMoneyTracker.ts -->
```typescript
export type Category =
  | "Alquiler" | "Supermercado" | "Entretenimiento" | "Salidas"
  | "Vacaciones" | "Servicios" | "Vestimenta" | "Subscripciones"
  | "Insumos" | "Estudio" | "Otros" | "Gym";

export interface Expense {
  id: string;
  date: string;
  name: string;
  amount: number;
  usdRate: number;
  category: Category;
  currencyType: CurrencyType;
  installments?: { total: number; current: number; startDate: string; };
}

export interface MonthlyData {
  salaries: { [key: string]: { amount: number; usdRate: number; }; };
  expenses: Expense[];
  extraIncomes: ExtraIncome[];
  investments: Investment[];
  usdPurchases: UsdPurchase[];
  salaryOverrides?: Record<string, { amount: number; usdRate: number }>;
  aguinaldoOverrides?: Record<string, { amount: number }>;
  transfers?: Transfer[];
}
```

<!-- Current migration version is 5 (set via `_migrationVersion: 5` in migrateData) -->
<!-- useLocalStorage pattern from hooks/useLocalStorage.ts -->
<!-- Separate localStorage key pattern: salaryHistory, incomeConfig, globalUsdRate -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Category type, Expense interface, and add new categories</name>
  <files>hooks/useMoneyTracker.ts, constants/colors.ts</files>
  <action>
  1. In `hooks/useMoneyTracker.ts`:
     - Add 4 new categories to the `Category` type union: `"Seguros" | "Impuestos" | "Transporte" | "Salud"`
     - Add `recurringId?: string` and `isPaid?: boolean` to the `Expense` interface (optional fields — no migration needed since they're optional)
     - Bump `_migrationVersion` from 5 to 6 in the migrateData function (no data transformation needed — just version bump)

  2. In `constants/colors.ts`:
     - Add 4 new entries to the CATEGORIES record:
       - `Seguros: { color: "rgb(244 63 94)" }` (rose-500)
       - `Impuestos: { color: "rgb(217 70 239)" }` (fuchsia-500)
       - `Transporte: { color: "rgb(14 165 233)" }` (sky-500)
       - `Salud: { color: "rgb(16 185 129)" }` (emerald-500)

  Note: Since recurringId and isPaid are optional fields, existing data works without transformation. The migration version bump is for tracking only.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Category type has 16 values, Expense has recurringId and isPaid optional fields, CATEGORIES has 16 entries with colors, migration version is 6, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create useRecurringExpenses hook with CRUD and auto-generation</name>
  <files>hooks/useRecurringExpenses.ts, hooks/useMoneyTracker.ts</files>
  <action>
  1. Create `hooks/useRecurringExpenses.ts`:
     - Define and export `RecurringStatus = "Activa" | "Pausada" | "Cancelada"` type
     - Define and export `RecurringExpense` interface with fields:
       - `id: string`
       - `name: string`
       - `amount: number`
       - `category: Category` (import from useMoneyTracker)
       - `currencyType: CurrencyType` (import from constants/investments)
       - `status: RecurringStatus`
       - `createdAt: string` (yyyy-MM format — first month to generate)
       - `pausedAt?: string` (yyyy-MM format — month when paused, cleared on resume)
     - Store recurring definitions in separate localStorage key `"recurringExpenses"` using `useLocalStorage<RecurringExpense[]>("recurringExpenses", [])`
     - Implement `addRecurring(data)`: creates RecurringExpense with `id: crypto.randomUUID()`, `status: "Activa"`, `createdAt: format(new Date(), "yyyy-MM")`
     - Implement `updateStatus(id, status)`: updates status. When setting "Pausada", also set `pausedAt: format(new Date(), "yyyy-MM")`. When setting "Activa" (resume), clear `pausedAt` to undefined
     - Implement `generateMissingInstances(allMonthlyData, globalUsdRate)`:
       - Get current month as `format(new Date(), "yyyy-MM")`
       - For each recurring with status "Activa":
         - Iterate months from `createdAt` to `currentMonth` (inclusive)
         - Skip months after `pausedAt` if set (this handles resumed recurrings that were paused mid-way)
         - For each month, check if instance exists: `monthExpenses.some(e => e.recurringId === rec.id && e.date.startsWith(month))`
         - If not exists, create Expense: `{ id: crypto.randomUUID(), date: "${month}-01", name: rec.name, amount: rec.amount, usdRate: rec.currencyType === CurrencyType.USD ? globalUsdRate : 0, category: rec.category, currencyType: rec.currencyType, recurringId: rec.id, isPaid: false }`
       - Return array of new expenses (caller decides how to add them)
     - Use date-fns `addMonths`, `format`, `parse` for month iteration (same pattern as installment date generation in useExpensesTracker)
     - Export the hook function `useRecurringExpenses` returning: `{ recurringExpenses, addRecurring, updateStatus, generateMissingInstances }`

  2. In `hooks/useMoneyTracker.ts`:
     - Import `useRecurringExpenses` from `./useRecurringExpenses`
     - Call `useRecurringExpenses()` inside `useMoneyTracker`
     - Run auto-generation: call `generateMissingInstances` passing globalUsdRate. The function handles reading/writing ALL months' localStorage keys internally (backfilling all missed months). For the currently loaded month, merge any new expenses into monthlyData.expenses via setMonthlyData. Guard with a `useRef` flag to prevent double-execution in React StrictMode (set ref to true after first run, check before running)
     - Expose `recurringExpenses`, `addRecurring`, `updateRecurringStatus` (renamed from updateStatus for clarity) in the useMoneyTracker return object
     - Also expose a `toggleExpensePaid(expenseId: string)` function that finds the expense by id and toggles its `isPaid` boolean (false→true, true→false, undefined→true)

  Important: The auto-generation should run as a useEffect with empty deps (or with recurringExpenses as dep), NOT on every render. Use a ref guard (`hasGeneratedRef`) to prevent StrictMode double-execution. The generation function needs access to ALL months' data (not just current month) to backfill missed months — iterate over each month key from createdAt to currentMonth and check/generate per month.

  Multi-month backfill implementation (per user decision — backfill ALL missed months silently): The generation function must iterate from each recurring's `createdAt` to `currentMonth` across ALL months, not just the currently loaded month. For each month, read its expenses from localStorage directly (using the `expenses_YYYY-MM` key pattern or however monthlyData keys are structured), check for existing instances, and write new expenses back to each affected month's localStorage key. Use the Research Pattern 3 logic from 06-RESEARCH.md as reference — iterate with date-fns `addMonths`, check `existingExpenses.some(e => e.recurringId === rec.id && e.date.startsWith(month))`, and write generated instances to the correct per-month storage. This ensures users who don't open the app for months get all recurring expenses retroactively generated on next load.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>useRecurringExpenses hook exists with addRecurring, updateStatus, generateMissingInstances. useMoneyTracker wires the hook and runs auto-generation on load, backfilling ALL months from createdAt to current month across localStorage keys. toggleExpensePaid flips isPaid on expenses. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles with no errors (`npx tsc --noEmit`)
2. Category type has 16 values (12 existing + 4 new)
3. Expense interface has optional recurringId and isPaid fields
4. RecurringExpense interface exists with all required fields
5. useRecurringExpenses hook provides CRUD + generation
6. useMoneyTracker exposes recurring operations and runs auto-generation
</verification>

<success_criteria>
- RecurringExpense data model fully defined with all fields from CONTEXT.md
- Auto-generation creates instances for active recurrings across ALL months from createdAt to current month (full backfill)
- Paused recurrings don't generate new instances
- New categories available with distinct colors
- All existing functionality unchanged (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/06-recurring-expenses/06-01-SUMMARY.md`
</output>
