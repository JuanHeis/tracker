---
phase: 23-reconciliar-disponible-del-resumen-con-saldo-liquido-calcula
plan: 03
type: execute
wave: 3
depends_on: [23-02]
files_modified:
  - lib/resumen/reconciliation.test.ts
  - lib/resumen/liquid-balance.ts
  - package.json
autonomous: true
requirements: [AC-1, AC-2, AC-3, AC-4, AC-6]
must_haves:
  truths:
    - "A fixture-driven vitest test loads the real backup and asserts June 2026 ARS Disponible == 28168.76 (toBeCloseTo, 2 dec), reconstructed independently from the backup — NOT computed by the code under test alone"
    - "The test also asserts the USD Disponible anchor for June 2026 equals the real USD liquid balance from the backup (a concrete stated number)"
    - "computeLiquidFlowForMonth is a FAITHFUL pure extraction of calculateAvailableForMonth/computeUsdAvailableForMonth (salary + extraIncomes − expenses + computeCashEffect); the both-engines-agree check is a consistency layer ON TOP of the concrete anchors, explicitly NOT the sole proof"
    - "sobranteAnterior for junio is DERIVED by chaining mayo through the reference engine on the real backup — not hardcoded"
    - "The cuadre-redundancy test uses the REAL backup which CONTAINS the $4.1M adjustment_ars, and asserts disponible == 28168.76 DESPITE it (result NOT off by ~4.1M)"
    - "package.json has a \"test\": \"vitest run\" script; _migrationVersion in the fixture is read-only; the test never writes the backup"
  artifacts:
    - path: "lib/resumen/reconciliation.test.ts"
      provides: "Concrete-anchor + both-engines consistency fixture test with June ARS 28168.76 + USD anchor + cuadre-redundancy"
      min_lines: 60
    - path: "package.json"
      provides: "test script"
      contains: "\"test\": \"vitest run\""
  key_links:
    - from: "lib/resumen/reconciliation.test.ts"
      to: "expense-tracker-backup-2026-06-30.json"
      via: "fixture import / fs.readFileSync"
      pattern: "expense-tracker-backup-2026-06-30"
    - from: "lib/resumen/reconciliation.test.ts"
      to: "lib/resumen/month-metrics.ts + lib/resumen/liquid-balance.ts"
      via: "asserts concrete anchors + both-engines consistency"
      pattern: "computeMonthMetrics"
---

<objective>
Build the permanent regression net: a fixture-driven vitest test using the REAL backup whose PRIMARY assertions are CONCRETE, independently-reconstructed anchors — June 2026 ARS Disponible == $28.168,76 and the USD Disponible == the real USD liquid balance — with a both-engines-agree consistency check layered on top. Extract a pure liquid-flow core to `lib/` as the reference engine. Add the `"test"` script.

Purpose: This test IS the phase's validation (VALIDATION.md central invariant). It fails today (numbers diverge), passes after Plans 01+02, and permanently guards against future divergence. The concrete independent anchors are what make it non-tautological — they catch a shared bug in computeCashEffect that a pure f(x)==f(x) both-engines check would miss.

Output: `lib/resumen/reconciliation.test.ts`, `lib/resumen/liquid-balance.ts`, `package.json` test script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-reconciliar-disponible-del-resumen-con-saldo-liquido-calcula/23-RESEARCH.md
@.planning/phases/23-reconciliar-disponible-del-resumen-con-saldo-liquido-calcula/23-VALIDATION.md
@lib/resumen/cash-effects.ts
@lib/resumen/month-metrics.ts

<interfaces>
Reconstruction facts (VERIFIED against expense-tracker-backup-2026-06-30.json):
- Backup shape: top-level `{ appName, exportVersion, exportDate, data }`; `data.monthlyData` holds `expenses, extraIncomes, investments, usdPurchases, transfers, loans, salaryOverrides`.
- payDay = 1, viewMode = "periodo", June range 2026-06-01..2026-06-30.
- Today's Resumen June ARS Disponible = 390668.76 ; TARGET = 28168.76 ; delta = 362500.00.
- delta = currency_ars_to_usd transfer { date:"2026-06-04", arsAmount:362500, usdAmount:250 }.
- May salary (effectiveDate "2026-05", amount 2364969) resolves for June via getSalaryForMonth.
- transfers array (6 total) INCLUDES two adjustment_ars: wizard seed { "2026-04-02", 360834.74 } and June cuadre { "2026-06-30", 4111230.13 (~$4.1M) }. wizardMonth = 2026-04 (.find picks the FIRST).
- Also two currency_ars_to_usd: { "2026-05-03", ars 28800, usd 20 } and { "2026-06-04", ars 362500, usd 250 }.

vitest style (from lib/resumen/month-metrics.test.ts): `import { describe, it, expect } from "vitest"`, `@/` alias to repo root, globals:true.
getFilterDateRange signature (hooks/usePayPeriod.ts): `getFilterDateRange(monthKey, viewMode, payDay) => { start: Date, end: Date }`.
</interfaces>
</context>

<decisions>
- **Q1 (balance-core extraction):** The reconciliation invariant per VALIDATION.md / RESEARCH is `computeMonthMetrics(...).disponible == <per-month cash-flow engine>(month)` where the per-month engine is `calculateAvailableForMonth` (ARS) / `computeUsdAvailableForMonth` (USD). Those are closures in the hook/component and not directly callable from a test. RESOLUTION: extract their pure core to `lib/resumen/liquid-balance.ts` — `computeLiquidFlowForMonth(...)` — a FAITHFUL extraction that independently sums salary + aguinaldo + extraIncomes − expenses and uses `computeCashEffect` ONLY for the transfers/loans/usdPurchases/investment-cash block. This mirrors the KNOWN-CORRECT calculateAvailableForMonth exactly (minus the closure).
- **Anchors are the proof, not both-engines equality:** The both-engines-agree assertion (`computeMonthMetrics(...).disponible - sobrante == computeLiquidFlowForMonth(...)`) is a CONSISTENCY check layered on top. On its own it is tautological (both call computeCashEffect). The NON-tautological proof is the CONCRETE anchor 28168.76 (ARS) and the concrete USD anchor, each reconstructed independently from the real backup billetera. State this explicitly in the test file header comment.
- Do NOT assert against `arsBalancePeriod` — it still carries the $4.1M cuadre fudge (RESEARCH Pitfall 1 / A2). Assert against the per-month cash-flow engine and the concrete billetera anchors only.
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Extract the faithful pure liquid-flow reference engine (computeLiquidFlowForMonth)</name>
  <files>lib/resumen/liquid-balance.ts</files>
  <read_first>
    - hooks/useMoneyTracker.ts L579-659 (calculateAvailableForMonth — the ARS per-month flow to mirror FAITHFULLY: salary + extraIncomes − expenses + cash block)
    - components/expense-tracker.tsx L272-308 (computeUsdAvailableForMonth — USD per-month flow to mirror)
    - lib/resumen/cash-effects.ts (computeCashEffect — reuse for the cash block; do NOT reimplement)
    - lib/resumen/month-metrics.ts (formula parity target for disponible)
    - 23-RESEARCH.md Open Question Q1 + "central reconciliation invariant"
  </read_first>
  <action>
Create `lib/resumen/liquid-balance.ts` with a pure function that FAITHFULLY reproduces the per-month cash-flow engine (calculateAvailableForMonth / computeUsdAvailableForMonth), reusing computeCashEffect for the transfers/loans/usdPurchases/investment-cash block only:
```typescript
import { CurrencyType } from "@/constants/investments";
import { computeCashEffect } from "@/lib/resumen/cash-effects";
import type { Transfer, Loan, UsdPurchase, Investment } from "@/hooks/useMoneyTracker";

export interface LiquidFlowInput {
  currency: CurrencyType;
  isInRange: (dateStr: string) => boolean;
  salaryAmount: number;      // ARS only; 0 for USD
  aguinaldoAmount: number;   // ARS only; 0 for USD
  extraIncomes: ReadonlyArray<{ amount: number; currencyType: CurrencyType; date: string }>;
  expenses: ReadonlyArray<{ amount: number; currencyType: CurrencyType; date: string }>;
  transfers: ReadonlyArray<Transfer>;
  loans: ReadonlyArray<Loan>;
  usdPurchases: ReadonlyArray<UsdPurchase>;
  investments: ReadonlyArray<Investment>;
}

/** Faithful pure core of calculateAvailableForMonth / computeUsdAvailableForMonth (single-period flow,
 *  EXCLUDING adjustments — the cuadre-free honest flow). Independently sums income − expenses, then
 *  adds computeCashEffect for the transfers/loans/usdPurchases/investment-cash block only. */
export function computeLiquidFlowForMonth(input: LiquidFlowInput): number {
  const { currency, isInRange, salaryAmount, aguinaldoAmount, extraIncomes, expenses,
          transfers, loans, usdPurchases, investments } = input;
  let total = salaryAmount + aguinaldoAmount;
  for (const inc of extraIncomes) if (inc.currencyType === currency && isInRange(inc.date)) total += inc.amount;
  for (const exp of expenses) if (exp.currencyType === currency && isInRange(exp.date)) total -= exp.amount;
  total += computeCashEffect({ currency, isInRange, transfers, loans, usdPurchases, investments });
  return total;
}
```
This is the honest, cuadre-free per-month flow. It intentionally does NOT add adjustment_ars/usd — that is why the reconciliation holds WITHOUT the cuadre (proving AC-3). It is the test's reference engine. Optionally refactor `calculateAvailableForMonth`/`computeUsdAvailableForMonth` to delegate to it for full single-source-of-truth, but only if zero-regression on the wizard-month display; if risky, keep it purely as the test's reference engine.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `lib/resumen/liquid-balance.ts` exists, exports `computeLiquidFlowForMonth` + `LiquidFlowInput`
    - `grep -n "computeCashEffect" lib/resumen/liquid-balance.ts` matches (reuses shared fn — cash block, no duplicated sign table)
    - `grep -n "salaryAmount + aguinaldoAmount" lib/resumen/liquid-balance.ts` matches (independently sums income — not derived from computeMonthMetrics)
    - `grep -n "adjustment" lib/resumen/liquid-balance.ts` returns nothing (cuadre-free flow — proves AC-3)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A pure, FAITHFUL computeLiquidFlowForMonth exists, independently summing income − expenses + computeCashEffect, representing the cuadre-free honest per-month flow.</done>
</task>

<task type="auto">
  <name>Task 2: Fixture reconciliation test — concrete ARS+USD anchors (primary), both-engines consistency (secondary), cuadre-redundancy on real 4.1M data + package.json script</name>
  <files>lib/resumen/reconciliation.test.ts, package.json</files>
  <read_first>
    - lib/resumen/month-metrics.test.ts (fixture/vitest style + @/ alias)
    - lib/resumen/liquid-balance.ts (reference engine, Task 1)
    - lib/resumen/month-metrics.ts (computeMonthMetrics — engine under test)
    - hooks/usePayPeriod.ts (getFilterDateRange for building isInRange)
    - expense-tracker-backup-2026-06-30.json (the fixture — `data.monthlyData`: expenses, extraIncomes, investments, transfers, loans, usdPurchases, salaryOverrides; transfers CONTAINS the $4.1M June adjustment_ars)
    - 23-RESEARCH.md "Fixture-driven reconciliation test" + "Reconstructing the June anchor" + Pitfall 1
    - 23-VALIDATION.md Central Invariant
  </read_first>
  <behavior>
    - Load the real backup (`data.monthlyData`). Build `isInRange` for June 2026 and for May 2026, payDay=1, viewMode="periodo", via getFilterDateRange(monthKey, "periodo", 1) + parse between start/end.
    - Resolve June salary from the backup (May effectiveDate 2364969 resolves for June — read/resolve it; hardcoding 2364969 is allowed ONLY with a comment sourcing it from the backup salaryOverrides/salary history).
    - PRIMARY assertion (non-tautological): June ARS `computeMonthMetrics(...).disponible` == 28168.76 (toBeCloseTo, 2 dec). This constant is the real billetera close ($28.168,76), reconstructed independently from the backup — it is NOT produced by the both-engines equality, so it catches a shared computeCashEffect bug.
    - PRIMARY USD anchor: assert June USD `computeMonthMetrics(...).disponible` == the real USD liquid balance for the period (compute/state the concrete number from the backup billetera — read it from the USD flow over June and state it as a constant with a sourcing comment).
    - sobranteAnterior for junio MUST be DERIVED by chaining mayo: call `computeLiquidFlowForMonth` for MAY (2026-05, isInRange for May) on the real backup to produce May's flow, chain it as junio's sobrante (do NOT hardcode a magic May number; keep the expected value only as a `//` sanity comment). Same for USD.
    - SECONDARY consistency check: `computeMonthMetrics(...).disponible - sobranteAnterior == computeLiquidFlowForMonth(June)` for ARS and USD. Comment explicitly that this is a consistency layer, and the concrete anchors above are what make the suite non-tautological.
    - CUADRE-REDUNDANCY (AC-3): the backup transfers CONTAIN the $4.1M June adjustment_ars. Assert that `computeMonthMetrics(...).disponible` (via computeCashEffect, which EXCLUDES adjustments) still equals 28168.76 — i.e. the correct number DESPITE the $4.1M cuadre sitting in the data. Add an explicit assertion/comment that the result is NOT off by ~4.1M (e.g. `expect(disponible).toBeLessThan(1_000_000)` plus the 28168.76 anchor), proving the cuadre is harmlessly ignored / redundant.
  </behavior>
  <action>
Create `lib/resumen/reconciliation.test.ts` (vitest globals, `@/` alias). Header comment must state: "The concrete anchors (28168.76 ARS, <USD number>) reconstructed independently from the real billetera are the non-tautological proof; the both-engines-agree assertions are a consistency layer on top."

Steps:
1. Import the backup. If `tsconfig.json` has `resolveJsonModule` (verify), use `import backup from "../../expense-tracker-backup-2026-06-30.json"`. Else `fs.readFileSync(path.resolve(__dirname, "../../expense-tracker-backup-2026-06-30.json"), "utf-8")` + `JSON.parse`. Document choice in a comment.
2. `const md = (backup.data ?? backup).monthlyData;` Pull `expenses, extraIncomes, investments, transfers, loans, usdPurchases`. Assert (sanity) `transfers.some(t => t.type === "adjustment_ars" && t.date === "2026-06-30")` so the test is provably running against data that CONTAINS the $4.1M cuadre.
3. Build `isInRangeJune` via getFilterDateRange("2026-06","periodo",1) and `isInRangeMay` via getFilterDateRange("2026-05","periodo",1), each with `parse(date,"yyyy-MM-dd",new Date())` between start/end.
4. Derive sobrante by chaining May (W-3 — NO hardcode):
   `const sobranteJunAars = computeLiquidFlowForMonth({ currency: ARS, isInRange: isInRangeMay, salaryAmount: <May salary>, aguinaldoAmount: 0, extraIncomes, expenses, transfers, loans, usdPurchases, investments });`
   (same shape for USD with salary 0 → `sobranteJunUsd`). Keep the expected numeric value only as a `//` sanity comment.
5. `it("June 2026 ARS Disponible anchor == 28168.76 (independent billetera reconstruction)")`:
   `const arsDisp = computeMonthMetrics({ monthKey:"2026-06", currency: ARS, sobranteAnteriorRaw: sobranteJunAars, salaryAmount: 2364969 /* May effectiveDate, from backup */, aguinaldoAmount: 0, expenses, extraIncomes, investments, transfers, loans, usdPurchases, isInRange: isInRangeJune }).disponible;`
   `expect(arsDisp).toBeCloseTo(28168.76, 2);` // real billetera close; = 390668.76 − 362500
6. `it("June 2026 USD Disponible anchor == <real USD liquid>")`: same with currency:USD, salaryAmount:0, sobrante = sobranteJunUsd; assert `toBeCloseTo(<USD number>, 2)` (state the concrete number in-code with a sourcing comment).
7. `it("ARS both engines agree (consistency layer)")`: `expect(arsDisp - sobranteJunAars).toBeCloseTo(computeLiquidFlowForMonth({currency:ARS, isInRange:isInRangeJune, salaryAmount:2364969, aguinaldoAmount:0, extraIncomes, expenses, transfers, loans, usdPurchases, investments}), 2);` + comment "consistency only — anchors above are the real proof". Add the USD twin.
8. `it("cuadre is redundant: disponible == 28168.76 DESPITE the $4.1M adjustment_ars in the data")`: assert `arsDisp` is `toBeCloseTo(28168.76, 2)` AND `expect(arsDisp).toBeLessThan(1_000_000)` with comment "if the $4.1M cuadre leaked in, this would be ~4.14M — proves computeCashEffect excludes it (AC-3)".
Then add `"test": "vitest run"` to `package.json` scripts. Do NOT modify any other script.
  </action>
  <verify>
    <automated>npx vitest run lib/resumen/reconciliation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/resumen/reconciliation.test.ts` exits 0
    - Test asserts `toBeCloseTo(28168.76, 2)` for June ARS disponible (grep the file for `28168.76`)
    - Test asserts a concrete USD Disponible anchor (grep for `currency: CurrencyType.USD` AND a numeric toBeCloseTo for USD)
    - sobrante is DERIVED by calling computeLiquidFlowForMonth for May (`grep -n "isInRangeMay" lib/resumen/reconciliation.test.ts` matches; no hardcoded May sobrante as test INPUT)
    - Cuadre-redundancy test runs against data containing the June adjustment_ars (grep for `adjustment_ars` AND `2026-06-30`) and asserts `toBeLessThan(1_000_000)` (or equivalent) plus the 28168.76 anchor
    - Test reads `expense-tracker-backup-2026-06-30.json` and never writes it (grep: no `writeFileSync`)
    - `grep -n "\"test\": \"vitest run\"" package.json` matches
    - `npx vitest run` (full suite) exits 0 — all existing + new tests green
  </acceptance_criteria>
  <done>The fixture reconciliation test passes: June ARS anchor 28168.76 (independent) + concrete USD anchor as PRIMARY proof, both-engines consistency as secondary, and cuadre-redundancy proven on the REAL $4.1M-containing data; sobrante chained from May; package.json has the test script; full suite green.</done>
</task>

</tasks>

<verification>
- `npx vitest run` full suite green (month-metrics, cash-effects, dual-balances-core, reconciliation, and the other existing test files)
- June 2026 ARS Disponible asserts 28168.76 (independent constant) + concrete USD anchor
- sobrante chained from May (not hardcoded)
- cuadre-redundancy proven against the real $4.1M adjustment_ars in the data
- `npm test` runs `vitest run`
</verification>

<success_criteria>
- Permanent reconciliation test green: concrete ARS ($28.168,76) + USD anchors as the non-tautological proof, both-engines consistency layered on top
- adjustment_ars cuadre proven redundant (AC-3) DESPITE the $4.1M sitting in the real data — result not off by ~4.1M
- sobrante derived by chaining May through the reference engine (no stale hardcode)
- No schema/migration change; backup read-only
</success_criteria>

<output>
After completion, create `.planning/phases/23-reconciliar-disponible-del-resumen-con-saldo-liquido-calcula/23-03-SUMMARY.md` documenting the final assertions (which are primary anchors vs secondary consistency), the exact June ARS/USD numbers reproduced and their billetera source, how May chaining derives the sobrante, and how the cuadre-redundancy test uses the real $4.1M data. Also note whether the balance-core extraction (Q1) was wired into the live engines or kept as a test-only reference.
</output>
</content>
